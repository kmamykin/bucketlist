<!DOCTYPE html>
<html>
<head>
  <title>Bucketlist</title>
  <meta charset='utf-8'>
  <%= stylesheet_link_tag "application", :debug => Rails.env.development? %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="fronty">
  <!-- THE TOP NAVBAR -->
  <div id="topLink"></div>
  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container-fluid">
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>

        <div class="nav-collapse">
          <div class="pull-right">
            <% if signed_in? %>
              <%= image_tag current_user.picture_url %>
              <%= current_user.name %>
            <% else %>
              <a class="btn btn-info btn-small round2" data-toggle="modal" href="#myModal">
                <i class="icon-white icon-user"></i> Sign In
              </a>
            <% end %>
          </div>
          <ul class="nav pull-right">
            <li> <%= link_to 'Submit Your Experience', new_experience_path, :class => "smooth" %> </li>
            <% if signed_in? %>
              <li> <%= link_to 'Signout', signout_path, :class => "smooth" %> </li>
            <% end %>
          </ul>
        </div>

      </div>
    </div>
  </div>
  <!-- SOCIAL MEDIA -->

  <!-- CONTAINER STARTS -->
  <div class="container-fluid pad58">
    <br/><br/>

    <div class="row-fluid">
      <div class="mainlogo">
        <%= image_tag "Bucket_List_2.png", :style => "margin-left: -240px;" %>
      </div>
    </div>

    <%= yield %>

  </div>

</div>

<div class="bgbox">
</div>

<div class="modal hide fade" id="myModal">
  <div class="modal-header">
    <a class="close" data-dismiss="modal">×</a>

    <h3>Sign in Details</h3>
  </div>
  <div class="modal-body">
    <h2>Signin with:</h2>

    <div>
      <%= link_to "Twitter", '/auth/twitter' %>
    </div>
    <div>
      <%= link_to "Facebook", '/auth/facebook' %>
    </div>
  </div>
  <div class="modal-footer">
    <a href="#" class="btn">Close</a>
    <a href="#" class="btn btn-primary">Save changes</a>
  </div>
</div>

<%= yield :javascripts %>

</body>
</html>
